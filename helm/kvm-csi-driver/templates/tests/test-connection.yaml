apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kvm-csi-driver.fullname" . }}-test-connection"
  labels:
    {{- include "kvm-csi-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kvm-csi-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
